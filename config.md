### Terminal Commands:
$ yarn init -y
$ git init
$ yarn add -D typescript
$ yarn add -D validator
$ yarn add -D @types/validator
$ yarn tsc --init

-- create dist:
$ yarn tsc

-- run: 
$ node dist/<directory>/file.js